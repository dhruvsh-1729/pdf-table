[
  {
    "prompt_key": "record.summary.primary",
    "scope": "record",
    "field_key": "summary",
    "variant": "primary",
    "title": "Record Summary",
    "description": "Used by record-level AI summary generation from extracted text.",
    "required_placeholders": ["label", "text"],
    "system_prompt": "You are an expert editor for academic PDF content. Use only the provided extracted text. Do not make up facts, add disclaimers, or include pre/post text. Keep output concise and accurate.",
    "user_prompt_template": "Create a short accurate summary (~300 words) of all details mentioned in {{label}}. Ensure no details are false, inaccurate, or hallucinated. After generating, review the summary against the PDF content to correct any mistakes, inaccuracies, or discrepancies. Use appropriate language for regular readers and research scholars - keep it sharp and concise without extra words. You may add relevant post-publication updates in brackets if applicable. Verify all information carefully before summarizing. Avoid bullet points and introductions like \"Sure\" or \"Summary:\".\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "record.summary.regen",
    "scope": "record",
    "field_key": "summary",
    "variant": "regen",
    "title": "Record Summary (Regenerate)",
    "description": "Alternate summary prompt used when regenerating a record summary.",
    "required_placeholders": ["label", "text"],
    "system_prompt": "You are an expert editor for academic PDF content. Use only the provided extracted text. Do not make up facts, add disclaimers, or include pre/post text. Keep output concise and accurate.",
    "user_prompt_template": "Rewrite with different wording and emphasis (avoid repeating prior phrasing). Create a short accurate summary (~300 words) of all details mentioned in {{label}}. Ensure no details are false, inaccurate, or hallucinated. After generating, review the summary against the PDF content to correct any mistakes, inaccuracies, or discrepancies. Use appropriate language for regular readers and research scholars - keep it sharp and concise without extra words. You may add relevant post-publication updates in brackets if applicable. Verify all information carefully before summarizing. Avoid bullet points and introductions like \"Sure\" or \"Summary:\".\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "record.conclusion.primary",
    "scope": "record",
    "field_key": "conclusion",
    "variant": "primary",
    "title": "Record Conclusion",
    "description": "Used by record-level AI conclusion generation from extracted text.",
    "required_placeholders": ["label", "text"],
    "system_prompt": "You are an expert editor for academic PDF content. Use only the provided extracted text. Do not make up facts, add disclaimers, or include pre/post text. Keep output concise and accurate.",
    "user_prompt_template": "Write a short, unique and distinctive conclusion (110-140 words) from {{label}}. Focus on key implications, outcomes, and significance rather than repeating summary content. Ensure the conclusion is specific to this document's findings and contributions. Output only the conclusion paragraph.\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "record.conclusion.regen",
    "scope": "record",
    "field_key": "conclusion",
    "variant": "regen",
    "title": "Record Conclusion (Regenerate)",
    "description": "Alternate conclusion prompt used when regenerating a record conclusion.",
    "required_placeholders": ["label", "text"],
    "system_prompt": "You are an expert editor for academic PDF content. Use only the provided extracted text. Do not make up facts, add disclaimers, or include pre/post text. Keep output concise and accurate.",
    "user_prompt_template": "Provide a fresh take (avoid repeating prior wording) and keep focus on implications. Write a short, unique and distinctive conclusion (110-140 words) from {{label}}. Focus on key implications, outcomes, and significance rather than repeating summary content. Ensure the conclusion is specific to this document's findings and contributions. Output only the conclusion paragraph.\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "record.tags.primary",
    "scope": "record",
    "field_key": "tags",
    "variant": "primary",
    "title": "Record Tags",
    "description": "Used by record-level AI tag generation from extracted text.",
    "required_placeholders": ["label", "text"],
    "system_prompt": "You are an expert editor for academic PDF content. Use only the provided extracted text. Do not make up facts, add disclaimers, or include pre/post text. Keep output concise and accurate.",
    "user_prompt_template": "Generate exactly 5 tags that best capture the essence of {{label}}. Each tag must be exactly 3 words, Title Case, and directly relevant to the PDF content only. Use only proper English letters (A-Z, a-z) and spaces - absolutely no special characters, symbols, asterisks, dashes, dots, or any punctuation marks in the tags. Avoid generic words (article, pdf, document). Return exactly 5 tags, one per line, with no additional text, explanations, or formatting.\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "record.tags.regen",
    "scope": "record",
    "field_key": "tags",
    "variant": "regen",
    "title": "Record Tags (Regenerate)",
    "description": "Alternate tag prompt used when regenerating record tags.",
    "required_placeholders": ["label", "text"],
    "system_prompt": "You are an expert editor for academic PDF content. Use only the provided extracted text. Do not make up facts, add disclaimers, or include pre/post text. Keep output concise and accurate.",
    "user_prompt_template": "Generate an alternate set (avoid generic or previously suggested words). Generate exactly 5 tags that best capture the essence of {{label}}. Each tag must be exactly 3 words, Title Case, and directly relevant to the PDF content only. Use only proper English letters (A-Z, a-z) and spaces - absolutely no special characters, symbols, asterisks, dashes, dots, or any punctuation marks in the tags. Avoid generic words (article, pdf, document). Return exactly 5 tags, one per line, with no additional text, explanations, or formatting.\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.name.primary",
    "scope": "split",
    "field_key": "name",
    "variant": "primary",
    "title": "Split Magazine Name",
    "description": "Extracts the magazine or publication name for a PDF split.",
    "required_placeholders": ["subject", "text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Identify the magazine or publication name {{subject}}. Output only the most likely magazine title (max 60 characters). If nothing is clear, return \"Unknown\".\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.volume.primary",
    "scope": "split",
    "field_key": "volume",
    "variant": "primary",
    "title": "Split Volume",
    "description": "Extracts the volume identifier for a PDF split.",
    "required_placeholders": ["subject", "text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Extract the volume identifier {{subject}}. Prefer formats like \"Volume 12\" or \"Vol. XII\". Output only one value. If not found, return \"Unknown\".\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.number.primary",
    "scope": "split",
    "field_key": "number",
    "variant": "primary",
    "title": "Split Number",
    "description": "Extracts the issue or edition label for a PDF split.",
    "required_placeholders": ["subject", "text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Extract the issue/number/edition label {{subject}}. Prefer forms such as \"Issue 3\", \"No. 3\", or \"Number 3\". Output only one value. If not found, return \"Unknown\".\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.timestamp.primary",
    "scope": "split",
    "field_key": "timestamp",
    "variant": "primary",
    "title": "Split Timestamp",
    "description": "Extracts the publication date for a PDF split.",
    "required_placeholders": ["subject", "text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Return the publication date {{subject}} as \"MMM YYYY\" (e.g., Jan 2024). If only a year is present, return the year. Use English month abbreviations. If unclear, return \"Unknown\".\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.title_name.primary",
    "scope": "split",
    "field_key": "title_name",
    "variant": "primary",
    "title": "Split Title",
    "description": "Drafts the best short article title for a PDF split.",
    "required_placeholders": ["subject", "text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Provide the best short article title {{subject}}. Keep it under 12 words, clear, and specific. Avoid publication names and filler words. If unclear, propose a precise 6-12 word title based only on the text.\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.title_name.regen",
    "scope": "split",
    "field_key": "title_name",
    "variant": "regen",
    "title": "Split Title (Regenerate)",
    "description": "Alternate short article title prompt for a PDF split.",
    "required_placeholders": ["subject", "text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Provide a fresh alternate wording. Provide the best short article title {{subject}}. Keep it under 12 words, clear, and specific. Avoid publication names and filler words. If unclear, propose a precise 6-12 word title based only on the text.\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.page_numbers.primary",
    "scope": "split",
    "field_key": "page_numbers",
    "variant": "primary",
    "title": "Split Page Numbers",
    "description": "Extracts the page range for a PDF split.",
    "required_placeholders": ["text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Report the page range covered by this split as numbers only. Use \"start-end\" (e.g., \"112-118\"). If it's a single page, return that number. If no range is clear, return \"Unknown\".\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.summary.primary",
    "scope": "split",
    "field_key": "summary",
    "variant": "primary",
    "title": "Split Summary",
    "description": "Drafts a concise factual summary for a PDF split.",
    "required_placeholders": ["text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Write a concise, factual summary (250-320 words) of this PDF content. Do not add introductions or bullets. Review against the text to avoid inaccuracies.\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.summary.regen",
    "scope": "split",
    "field_key": "summary",
    "variant": "regen",
    "title": "Split Summary (Regenerate)",
    "description": "Alternate summary prompt used when regenerating a split summary.",
    "required_placeholders": ["text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Use different phrasing from prior attempts. Write a concise, factual summary (250-320 words) of this PDF content. Do not add introductions or bullets. Review against the text to avoid inaccuracies.\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.conclusion.primary",
    "scope": "split",
    "field_key": "conclusion",
    "variant": "primary",
    "title": "Split Conclusion",
    "description": "Drafts a conclusion paragraph for a PDF split.",
    "required_placeholders": ["text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Write a short conclusion paragraph (110-140 words) focused on implications and outcomes specific to this document. Output only the paragraph.\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.conclusion.regen",
    "scope": "split",
    "field_key": "conclusion",
    "variant": "regen",
    "title": "Split Conclusion (Regenerate)",
    "description": "Alternate conclusion prompt used when regenerating a split conclusion.",
    "required_placeholders": ["text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Offer a distinct perspective (no repeated phrasing). Write a short conclusion paragraph (110-140 words) focused on implications and outcomes specific to this document. Output only the paragraph.\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.tags.primary",
    "scope": "split",
    "field_key": "tags",
    "variant": "primary",
    "title": "Split Tags",
    "description": "Generates tags for a PDF split.",
    "required_placeholders": ["text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Generate exactly 5 tags that capture this content. Each tag must be 2-3 words, Title Case, no punctuation or special characters. One tag per line; no extra text.\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.tags.regen",
    "scope": "split",
    "field_key": "tags",
    "variant": "regen",
    "title": "Split Tags (Regenerate)",
    "description": "Alternate tag prompt used when regenerating split tags.",
    "required_placeholders": ["text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Suggest an alternate set without repeating prior wording. Generate exactly 5 tags that capture this content. Each tag must be 2-3 words, Title Case, no punctuation or special characters. One tag per line; no extra text.\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.authors.primary",
    "scope": "split",
    "field_key": "authors",
    "variant": "primary",
    "title": "Split Authors",
    "description": "Extracts author names for a PDF split.",
    "required_placeholders": ["subject", "text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Extract the author(s) {{subject}} from the text. Return only author names, one per line. Use the exact name spellings from the text. If no author is present, return \"Unknown\".\n\nExtracted text:\n{{text}}"
  },
  {
    "prompt_key": "split.authors.regen",
    "scope": "split",
    "field_key": "authors",
    "variant": "regen",
    "title": "Split Authors (Regenerate)",
    "description": "Alternate author extraction prompt for a PDF split.",
    "required_placeholders": ["subject", "text"],
    "system_prompt": "You are an expert metadata extractor for magazine PDFs. Use only the provided extracted text. Do not invent facts, add disclaimers, or include any labels. Return clean plain text only.",
    "user_prompt_template": "Return a fresh list. Extract the author(s) {{subject}} from the text. Return only author names, one per line. Use the exact name spellings from the text. If no author is present, return \"Unknown\".\n\nExtracted text:\n{{text}}"
  }
]
